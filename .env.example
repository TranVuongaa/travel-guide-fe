# Public browser configuration. These values are visible to every user.
NEXT_PUBLIC_API_BASE_URL=http://52.62.25.92

# Optional. Google login/link stays unavailable until both values are configured.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
NEXT_PUBLIC_GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback
